name: My First Workflow

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print Message
        #run: echo "Hello GitHub Actions"
        run: ll 
